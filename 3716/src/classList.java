public class classList {
	
	/*
	 * A change
	 */
	studentArray classList;

	public void loadClass() {
		/*
		 * Loads the classList from a saved file
		 */
	}

	public void buildClass() {
		/*
		 * Used only if there is no saved classList.
		 * 
		 * Takes students from provided database and creates a classList of
		 * student objects
		 * 
		 * Never going to happen
		 */
	}

	public void saveClassList() {
		/*
		 * Used to save the classList to the Instructors system
		 */
	}

	public studentArray getClassList() {
		return classList;
	}
}
